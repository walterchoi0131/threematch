# 法師玩法 Muse

## 設計目標

法師不是「另一個高 ATK 輸出」，而是一種改寫出傷節奏的職業型態。

目前遊戲的核心出傷很清楚：消除某元素寶石後，同元素角色依 `寶石數 × ATK × 元素克制` 出傷；回應技能則在消除數、形狀、upper gem、轉色、延遲結算上改寫這條路徑。法師玩法應該站在這套系統上，而不是另開一套完全不相容的戰鬥規則。

我想讓法師的關鍵差異是：

- 普通消除不只是立即打人，也會累積可被設計、保存、放大的「法力」。
- 法師更重視 `Magic`，讓目前已有但較少直接承擔主輸出的 `base_magic/get_magic()` 成為核心。
- 法師的爆發有準備期，玩家需要選擇何時儲能、何時釋放、何時冒過載風險。
- 法師應該與現有 upper gem 連鎖、主動技選格、回應技能門檻、戰鬥日誌、傷害數字共用表現管線。

## 目前出傷系統觀察

### 普通攻擊

- `BattleManager.get_attack_data(gem_type, count)` 會找出所有同 `gem_type` 的角色。
- 每名同元素角色造成 `character.get_atk() × count`。
- 元素克制目前程式實作為火克葉、葉克水、水克火，倍率 1.5。
- 光暗互剋在世界觀中存在，但目前核心公式未實作；法師 v1 最好先不依賴光暗互剋。
- `pending_skill_blasts` 可以把技能儲存的額外寶石數併入下一次同色攻擊，這很適合做「儲能後下次施法」的基礎。

### 回應技能

- `responding_skills` 目前支援 `count` 與 `line` 觸發。
- 同一筆 blast 只取優先級最高的回應技能。
- 回應技能可以生成 upper gem、轉色、或在後續流程產生效果。
- 融合類回應技能不消耗回合，會先飛粒子到點擊位置，再放置 upper gem。

### Upper Gem 與連鎖

- upper gem 點擊後會進入連鎖爆炸結算。
- 連鎖倍率是 `1 + (chain_count - 1) × 0.10`。
- 某些 upper gem 有獨立公式，例：聖十字會以自己的公式計算傷害與治療。
- 這代表法師可以有「連鎖越長，法術倍率越高」或「連鎖不是立刻打，而是轉為法力」兩種方向。

### Magic 現況

- `CharacterData` 已有 `base_magic`、`magic_growth`、`get_magic()`。
- 現況中 Magic 已被部分技能用於持續型 upper gem、治療倍率或特殊效果。
- 法師可以成為第一個把 Magic 作為主出傷係數的角色型態。

## 核心玩法提案：詠唱與法力

### 核心循環

1. 玩家消除法師元素的寶石。
2. 法師造成較低的普通 ATK 傷害，並累積法力。
3. 法力達到門檻後，觸發回應技能或強化下一次主動技。
4. 玩家用主動技選擇法陣位置、目標形狀或釋放時機。
5. 法術用 `Magic × 法力 × 係數` 結算，並可吃連鎖倍率或元素克制的一部分。

### 為什麼這和現有角色不同

- 普通輸出型：消越多，立刻打越多。
- 轉色型：改盤面，幫下一次消除更大。
- Upper gem 型：達門檻後生成可點擊爆破物。
- 法師型：把消除轉成可保存的法術資源，再用選格或時機決定價值。

## 資源模型

### 法力 Charge

每個法師有自己的 `charge`，不建議做全隊共用，否則會很快變成隊伍通用能量條。

建議 v1 規則：

- 消除同元素普通寶石時，法師獲得 `count` 點 charge。
- upper gem 連鎖中爆到同元素寶石時，也可以獲得 `floor(count × 0.5)` charge，避免連鎖過度膨脹。
- charge 上限 18 或 24。
- charge 滿時不再增加，或轉為「過載」。

### 過載 Overload

過載是法師玩法的風險按鈕。

可選規則：

- charge 超過上限時，多出來的值變成 overload。
- overload 會提高下一次法術傷害，但每點 overload 使玩家受少量反噬或使法術不穩定。
- 若不想加玩家反傷，overload 可以改成在棋盤生成 1 個不穩定法印，需要玩家處理。

我比較喜歡反噬或失控格，因為它讓法師有「貪傷害」的個性，而不是純收益。

### 法術槽 Spell Slot

比單純 charge 更進階的版本：

- 每 6 charge 形成 1 個 spell slot。
- 最多保存 3 個 slot。
- 主動技會依消耗 slot 數改變範圍、段數、或係數。
- `6/12/18` 的節點也方便 UI 顯示，像 3 顆小符文燈。

這比直接顯示 0-24 charge 更直覺，但實作 UI 略多。

## 出傷模型

### 低 ATK，高 Magic

法師的 `base_atk` 應偏低，避免普通消除已經高傷又額外存法力。

建議定位：

- ATK：同等級輸出角的 55%-70%。
- Magic：同等級平均的 120%-150%。
- HP：偏低或中低。

### 基礎法術公式

建議先用簡單公式：

```text
spell_damage = Magic × spent_charge × spell_coeff × target_modifier × chain_modifier
```

其中：

- `spent_charge`：本次消耗的法力。
- `spell_coeff`：技能係數，通常 0.7-1.4。
- `target_modifier`：單體 1.0，多體 0.45-0.65，隨機多段 0.7。
- `chain_modifier`：可吃現有 upper gem chain multiplier，但上限建議 1.5。

### 與普通出傷的差異

普通出傷是 `count × ATK`，鼓勵一次消很多。

法術出傷是 `累積 charge × Magic`，鼓勵跨回合規劃與選擇釋放窗口。

### 不建議的公式

```text
Magic × count × charge
```

這會讓消除數與存量相乘，爆發很容易失控。若真的要用，必須有很低係數與嚴格上限。

## 三種法師方向

### 1. 詠唱法師

核心：消除寶石累積詠唱，達門檻後自動或手動釋放。

玩法感：慢熱、爆發、怕被打斷。

技能靈感：

- 被動：每次消除同元素寶石，獲得 charge。
- 回應：消除 6+ 同元素時，如果 charge >= 12，下一次法術係數 +25%。
- 主動：消耗所有 charge，對目標造成 `Magic × charge × 1.0`。
- 風險：若連續 2 回合沒有消到同元素，charge 衰退 3 點。

適合第一版，因為它最少需要棋盤新物件。

### 2. 法陣法師

核心：主動技選格放置法陣，後續消除法陣範圍內寶石會轉為法術傷害。

玩法感：預判盤面、設陷阱、等 combo 經過。

技能靈感：

- 主動：選一格建立 3x3 法陣，持續 2 回合。
- 法陣內寶石被消除時，額外累積 charge。
- 法陣結束時釋放，傷害依期間內消除數與 Magic 計算。
- 若法陣中心是 upper gem，結束時額外觸發一次小爆發。

這個方向很符合新抽象的 board-selection active skill pipeline。

### 3. 連鎖法師

核心：法師不是直接創造 upper gem，而是把 upper gem 連鎖轉成法術共鳴。

玩法感：喜歡大連鎖，吃操作熟練度。

技能靈感：

- 被動：每次 upper gem chain +1，獲得 1 層 resonance。
- 結算時，每層 resonance 讓下一次同元素攻擊或法術 +10%。
- 主動：把 resonance 全部轉成多段法術，每層打一段小傷害。
- 限制：resonance 回合結束清空，避免長期堆疊。

這方向很爽，但比較吃現有連鎖平衡，建議不是 v1。

## 推薦 v1：法陣詠唱混合

我推薦第一個可實作版本叫「奧術法陣」。

核心理由：

- 會用到現有 board selection pipeline。
- 會用到 Magic 作為主要輸出。
- 不必新增寶石種類。
- 不必立刻改普通 `get_attack_data` 公式。
- 玩家一眼能懂：選一塊區域，之後在那裡消寶石，法術會變強。

### V1 技能組

#### 被動：奧術蓄能

消除法師元素寶石時，法師獲得 charge。

```text
charge_gain = blasted_count
charge_cap = 18
```

如果該次消除位於法陣內，額外 +50% charge。

#### 回應：魔力凝結

當單次消除 6+ 法師元素寶石時，如果 charge >= 12，在最後點擊位置生成「魔晶」upper gem。

魔晶不一定要第一版就實作成新 upper type，也可以先用主動技直接消耗 charge。

若做成 upper gem：

- 點擊魔晶時消耗 12 charge。
- 造成十字或 3x3 法術爆炸。
- 傷害依 `Magic × 12 × 0.9` 計算。
- 若連鎖爆到其他 upper gem，吃現有 chain multiplier。

#### 主動：奧術法陣

CD：3 或 4 回合。

流程：

1. 點角色卡進入 selection mode。
2. 選一格，以該格為中心建立 3x3 法陣，持續到玩家下一次成功消除後結算，或持續 2 回合。
3. 法陣期間，範圍內被消除的寶石都記錄為 `circle_count`。
4. 結算時造成：

```text
damage = Magic × (spent_charge + circle_count) × 0.8
```

5. 若 `circle_count >= 5`，返還 3 charge。

### 為什麼好玩

- 玩家會先看盤面哪裡快要連起來，再放法陣。
- 放錯位置不是完全沒用，但會弱很多。
- 玩家可以選擇先存 charge，再放大招；或先放法陣，逼自己在區域內操作。
- 它和轉色角色有自然合作：轉色能把法陣區域變成更容易消的顏色。

## 技能原型池

### 主動技能

#### 奧術法陣

選 3x3 區域建立法陣。下一次玩家在區域內消除後，消耗 charge 造成 Magic 傷害。

#### 星屑飛彈

消耗最多 12 charge，分成 3-6 段攻擊目前目標。每段傷害 `Magic × 2`，段數依 charge 增加。

優點是很好表現，也能沿用現有 projectile/damage number 管線。

#### 元素抄寫

選一格，把該格周圍的寶石轉成法師元素，但每轉 1 顆消耗 1 charge。若 charge 不足，只轉最靠近中心的幾顆。

這是轉色型法師，比純輸出更穩。

#### 魔力反照

本回合下一次普通攻擊不立刻造成全部傷害，而是在敵人行動前追加一次 `Magic × 消除數 × 0.7` 的法術追擊。

這會改寫時間點，適合做「延遲傷害」教學。

#### 過載釋放

消耗全部 charge 與 overload，造成大傷害。每點 overload 讓傷害 +8%，但施放後玩家受到最大 HP 2% 的反噬，或在棋盤生成不穩定格。

#### 咒式封印

選一名敵人，使下一次法術傷害附帶敵人 CD +1。這不是純傷害，而是控制型法師。

### 回應技能

#### 魔力凝結

消除 6+ 同元素，獲得額外 charge；若 charge 滿，生成魔晶。

#### 幾何共鳴

消除 4+ 直線時，該次 charge gain +50%。如果是 5 連以上，下一個法術範圍 +1。

#### 法術回音

本回合第二次消除法師元素時，記錄第二次消除數。回合結束前若法師施法，追加 `Magic × 第二次消除數 × 0.5`。

#### 星圖校準

若同一回合內消除過 2 種以上不同元素，法師下一次法術吃 `+15%` 係數。這能鼓勵多色盤操作。

#### 秘文連鎖

upper gem chain count >= 3 時，獲得 `chain_count` charge。這讓法師能搭上 upper gem 玩法，但不直接放大到失控。

### 被動技能

#### 奧術蓄能

同元素消除給 charge。最直覺。

#### 魔力保存

回合結束時保留 charge，但若本回合沒有任何同元素消除，失去 2 charge。

#### 精密詠唱

法術不吃元素克制，但對被鎖定目標有 +20% 傷害。這能讓法師更像單點專精。

#### 廣域詠唱

法術可打全體，但傷害係數降低。適合另一名法師分支。

## 法師與元素的關係

法師不一定要是新元素。比較好的做法是「任何元素都可以有法師型角色」。

### 火法

- 高爆發，過載風險高。
- 擅長把 charge 轉成單次大傷害。
- 法陣可破壞可破壞障礙，與現有 BREAK 概念相容。

### 水法

- 儲能穩定，偏控制與延遲傷害。
- 可把部分法術傷害轉成治療或防護。
- 適合搭配 `pending_skill_blasts`，把水寶石儲存到下一次出手。

### 葉法

- 擅長在棋盤上種法印，靠下一次消除觸發。
- 可以和轉色、生息、持續型 upper gem 合作。
- 爆發較慢，但盤面控制強。

### 光法

- 擅長範圍、淨化、十字法陣。
- 可與現有十字選格手感一致。
- 適合做教學法師，因為視覺最清楚。

### 暗法

- 擅長延遲、標記、生命交換、過載。
- 可以把一部分玩家風險換成更高輸出。
- 等暗元素正式玩法穩定後再做較好。

## UI / UX 想法

### 角色卡 Charge 表示

最小可行：角色卡右下角顯示 0-18 的小數字與三段亮點。

更好版本：

- 三顆小符文，每 6 charge 點亮一顆。
- 滿 charge 時角色卡邊框有低頻呼吸光。
- overload 時邊框變成不穩定閃爍，但不要遮住 cooldown。

### 法陣預覽

可直接沿用 active skill selection dim overlay：

- 進入選格時全畫面暗化。
- 可選中心格保持正常。
- hover 時顯示 3x3 或十字範圍。
- 角色卡顯示取消 overlay。

### 戰鬥日誌格式

建議法師日誌直接明示 charge 與 Magic，玩家才知道傷害從哪來。

```text
奧術法陣：⚔? 改為 ✦12 × MAG 18 ×0.8 = 172
過載釋放：Charge 18 + Overload 3 × MAG 18 = 430
```

若要避免新符號，先用文字：

```text
Arcane Circle：21 Mana × Magic 18 = 302
```

### 傷害數字

法術傷害可以使用更高層級的字體效果，但不必全部做 super。

建議區分：

- 普通法術：元素色數字 + 微弱外框。
- 滿 charge：加白色內光。
- overload：加抖動或二段 pop。

## VFX 想法

### 法力吸收

當消除法師元素寶石時，粒子不是只飛到角色卡，也可以有少量細線留在卡片周圍，像 charge 被吸收。

### 法陣

棋盤格上畫半透明圓環或方形 glyph，不要蓋住寶石辨識。

建議第一版用簡單 `ColorRect/Line2D` 或貼圖 overlay，避免引入太重的特效。

### 施法

釋放時從角色卡到法陣中心，再從法陣中心到敵人。這能讓玩家理解「我剛剛選的區域正在施法」。

### 過載

法陣邊緣快速閃爍，然後爆開；玩家若受反噬，角色卡跳出小紅字。

## 實作切片

### Slice 1：純 Charge 被動

目標：先驗證法師是否有好玩的節奏。

- 在 `main.gd` 或 `battle_manager.gd` 新增 `wizard_charge: Dictionary`。
- 用 skill name 或新 tag 判斷角色是否是法師。
- 在普通 blast worker 結算時，如果 blast gem_type == 法師 gem_type，增加 charge。
- 在 character panel 顯示 charge 數字。
- 暫時不改出傷，只看玩家是否能感受到資源累積。

### Slice 2：主動技消耗 Charge

目標：讓 charge 變成真正的出傷。

- 新增主動技 `Arcane Release`。
- 點角色卡後直接消耗 charge，對目前目標造成 `Magic × charge × 1.0`。
- 成功施放才進 CD。
- charge 低於 6 時不能施放，或施放但係數低。

### Slice 3：法陣選格

目標：把法師玩法從「能量條按鈕」提升成棋盤玩法。

- 新增主動技 `Arcane Circle`。
- 使用 `_run_board_selection_active_skill` 進入選格。
- pattern 可先用 `cross` 或新增 `square_3x3`。
- 選定後記錄法陣範圍，下一次消除時計算範圍內消除數。
- 結算法術傷害。

### Slice 4：Overload

目標：加入法師的風險與辨識度。

- charge 超過 cap 後轉 overload。
- overload 提高 spell coeff。
- 施放後扣玩家 HP 或生成不穩定格。
- UI 顯示 overload 狀態。

## 需要小心的平衡問題

### 不要讓法師免費吃兩套傷害

如果法師普通攻擊照常完整出傷，又額外累積 charge，會變成純強化版輸出。

解法：

- 法師 ATK 較低。
- charge 法術需要主動技 CD 或門檻。
- 部分 charge 會衰退。
- 法術不完全吃元素克制。

### 不要讓 charge 永久囤到 Boss 秒殺

解法：

- charge cap 18 或 24。
- 過關不保存 charge。
- 換波可以保留一部分，但不能全保留。
- overload 有風險。

### 不要讓法陣阻礙讀盤

法陣 overlay 透明度要低，寶石顏色與 upper gem icon 必須仍然清楚。

### 不要讓回應技能互相搶優先級變得不可控

目前同一筆 blast 只執行最高優先級回應技能。法師若也靠 responding skill，需要決定它和 fusion skill 的關係。

建議：

- 法師 charge 被動不要放進 `responding_skills`，避免和 upper gem 生成互斥。
- 只有「生成魔晶」這類大效果才放 responding skill。
- priority 應低於該元素核心 upper gem，除非法師角色本來就是該元素主核心。

## 關卡教學構想

### 教學 1：蓄能

給玩家 5x5 或 6x6 盤面，讓他消一次法師元素，角色卡上 charge 點亮。

提示重點：消除同元素會充能。

### 教學 2：釋放

讓 charge 到 12，角色卡主動技亮起。玩家點擊角色卡直接釋放單體法術。

提示重點：Magic 是法師爆發來源。

### 教學 3：法陣

給一個明顯即將成形的 3x3 區域，讓玩家放法陣後消除其中寶石。

提示重點：法師不是只按技能，也要讀盤。

### 教學 4：過載

讓玩家看到 charge 滿了還繼續充能，產生 overload。第一次 overload 可以只加傷害不懲罰，下一關再加入反噬。

## 可命名方向

### 技能名

- 奧術蓄能
- 奧術法陣
- 星屑飛彈
- 魔力凝結
- 法術回音
- 幾何共鳴
- 過載釋放
- 咒式封印
- 元素抄寫
- 秘文連鎖

### 狀態名

- Charge / 法力
- Spell Slot / 法術槽
- Resonance / 共鳴
- Overload / 過載
- Circle / 法陣
- Rune / 法印

## 最想先做的版本

若只做一個最小但有辨識度的法師，我會這樣切：

角色定位：光或暗元素法師，高 Magic、低 ATK、中低 HP。

被動：奧術蓄能

- 消除同元素寶石獲得 charge。
- charge cap 18。
- 法師普通 ATK 偏低。

主動：奧術法陣

- CD 4。
- 選一格建立 3x3 法陣。
- 立刻消耗最多 12 charge。
- 法陣範圍內寶石轉為法師元素，並對目標造成 `Magic × spent_charge × 0.9`。
- 若 spent_charge >= 12，額外在中心生成魔晶或追加一次小爆炸。

回應：魔力凝結

- 單次消除 8+ 同元素時，如果 charge >= 12，生成魔晶 upper gem。
- 魔晶點擊後造成小範圍法術爆炸，傷害用 Magic，不用 ATK。

這個版本有三個清楚賣點：

- 平常消除是在準備爆發。
- 主動技要選盤面區域。
- 高門檻消除會生成法師專屬 upper gem。

## 後續延伸

### 多法師隊伍

如果未來有多名法師，不建議共用 charge。每名法師獨立充能，避免 UI 與平衡失控。

可以做「同隊第二名法師」的協同：

- A 法師施法後，B 法師獲得 2 charge。
- 本回合每施放一個法術，下一個法術係數 -20%，避免連發爆炸。

### 法術書系統

較遠期可以讓法師裝備 2-3 個法術，像小型技能組：

- 左鍵角色卡：預設主動技。
- 長按或詳情頁：切換當前法術。
- 每個法術共享 charge，但有不同 CD。

這很有深度，但會牽涉 UI 與角色養成，應該等核心法師手感成立後再做。

### 敵方反制

敵人可以有「打斷詠唱」「吸收法陣」「封鎖法印」等機制。

不過第一批法師關卡不要馬上反制太重，先讓玩家享受新玩法。

## 一句話總結

法師玩法的最好方向是：把目前的 `寶石數 × ATK` 立即出傷，改造成 `寶石數累積法力 → 讀盤選法陣 → Magic 結算爆發` 的延遲型出傷。它不需要推翻現有系統，反而能把已存在的 Magic、board selection、upper gem chain、pending blasts、戰鬥日誌全部串成一條新體驗。